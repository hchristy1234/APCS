public class nov16 {
    private int size;
    private sock footmitten;

    public shoes () {
        size = 7;
        footmitten = new sock();
    }

    // primitive: int, double, boolean
    // object: arrays, strings, arraylist, integer, shoes, scanner, double
    String Brian = "Saturday School";
    String Patrick = new String("Sunday School");

    double a = 4.8;
    // cast from primitive to object type double
    Double b = new Double(a);
    // cast from object type double to primitive
    double c = b.doubleValue();

    // SAME STUFF W INTEGERS
    int d = 5;
    Integer e = new Integer(d);
    int f = e.intValue();
}