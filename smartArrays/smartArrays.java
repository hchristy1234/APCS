import java.util.*;
public class smartArrays {
    public static void main(String[] args) {
        /*
        Add([], #): add a # to the end of the array
        Remove([]): remove a # from the end of an array
        Add([], spot, value): add a value to a given spot, push everything out
        Remove ([], spot): remove a value from spot and shrink the []
        Replace([], spot, value): Replace spot with new value
        indexOf([], #): returns first spot of item or -1 if it is not found
        */
        Scanner scan = new Scanner(System.in);
        System.out.println("how many values in your array?");
        
    }
}
